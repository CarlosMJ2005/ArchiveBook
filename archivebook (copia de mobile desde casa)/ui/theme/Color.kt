package com.example.pmdm.archivebook.ui.theme

import androidx.compose.ui.graphics.Color

// --- PALETA LUZ (MODO CLARO) ---
val LightPrimary = Color(0xff84240c)    // Granate principal
val LightOnPrimary = Color(0xfffce8cc)  // Crema sobre granate
val LightSecondary = Color(0xFF5200FF)  // Azul/Morado para acentos

val LightBackground = Color(0xFFfce8cc) // Fondo crema

// --- PALETA OSCURA (MODO OSCURO) ---
val DarkPrimary = Color(0xfffce8cc)     // Crema (ahora es el principal)
val DarkOnPrimary = Color(0xff84240c)   // Granate sobre crema
val DarkSecondary = Color(0xFF4C00FF)
val DarkBackground = Color(0xFF631C08)  // Fondo granate oscuro
